# odin-recipes
Build a basic HTML website containing recipes

The website will have an index page and others linked to it containing the recipes.

The style won't be over the top as we are only using HTML for styling.

A basic understanding of HTML and it's commands is required to complete this project (boilerplate; headings; paragraphs; lists).
